% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{mk_tbl_std}
\alias{mk_tbl_std}
\title{Create a Standardized Table of Significant Variables from AOV Results}
\usage{
mk_tbl_std(path, lst_prot_vars)
}
\arguments{
\item{path}{Character string. Path to the folder containing AOV result files in \code{.xlsx} format.}

\item{lst_prot_vars}{Character vector. List of protocol variables to check for significance.}
}
\value{
A tibble in wide format with one row per exposure and one column per protocol variable.
The value \code{"X"} indicates that the variable was significantly associated (p < 0.2) with the exposure.
Empty cells mean non-significant or missing data.
}
\description{
This function scans a directory for AOV result files (in \code{.xlsx} format, ending with \verb{_aov.xlsx}),
extracts protocol variables with a p-value < 0.2, and creates a wide-format summary table.
Each row corresponds to an exposure and each column to a protocol variable.
}
\examples{
\dontrun{
path_to_results <- "outputs/aov_results/"
prot_vars <- c("batch", "season", "storage_time")
mk_tbl_std(path_to_results, prot_vars)
}
}
